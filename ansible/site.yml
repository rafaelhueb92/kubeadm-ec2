---
- name: Ping Control Planes
  hosts: control_planes
  become: true
  vars:
    ansible_connection: aws_ssm
    ansible_aws_ssm_bucket_name: not-so-simple-ssm
  tasks:
    - name: Ping Control Planes
      ansible.builtin.ping:
