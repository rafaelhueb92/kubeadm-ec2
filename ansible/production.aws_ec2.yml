plugin: amazon.aws.aws_ec2
profile: default
regions:
  - us-east-1
groups:
  control_planes: "'production-control-plane' in ec2_tags.Name"
  workers: "'production-worker'in ec2_tags.Name"
hostnames:
  - instance-id
